Le paramètre "chunksize" limite la mémoire utilisée pour les actions
qui auront peut être besoin de beaucoup de ressources. Par exemple,
pour une valeur de 100, les adresses seront traitées par groupe de
100. De plus grandes valeurs feront que les commandes s'exécuteront un
peu plus vite et utiliseront plus de mémoire. Des valeurs plus petites
feront que les commandes s'exécuteront un peu plus lentement et
utiliseront moins de mémoire.
