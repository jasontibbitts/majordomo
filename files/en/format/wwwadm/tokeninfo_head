<!-- Majordomo tokeninfo_head format file -->
<table>
  <tr><td>Token  <td>$TOKEN
  <tr><td>Created<td>$DATE
  <tr><td>By     <td>$USER
  <tr><td>Type   <td>$TYPE
  <tr><td>Command<td>$CMDLINE
  <tr><td>Expires<td>$EXPIRE
  <tr><td>Reason <td>$REASONS
</table>

<form method=POST action="$CGIURL">
  <input type=submit name=func value=accept>
  <input type=submit name=func value=accept-archive>
  <input type=submit name=func value=reject>
  <input type=hidden name=domain value="$DOMAIN">
  <input type=hidden name=extra value="$TOKEN">
  <input type=hidden name=list value="$LIST">
  <input type=hidden name=passw value="$PASSWORD">
<br>
If you wish to make known your reasons for $HELP:ACCEPTing
or $HELP:REJECTing the request, type them here:
<br>

  <textarea rows=4 cols=70 name=xplanation>
  </textarea>

<ul>
  <li>The reasons are never shown for accepted, posted messages.
  <li>The reasons are never shown for rejected <b>confirm</b> requests.
  <li>Acknowledgements for rejected <b>consult</b> requests are sent only
      if the <b>ackreject</b> flag is set.  See the $HELP:SET and 
      $HELP:CONFIGSET_NONMEMBER_FLAGS help pages for more details.
</ul>
<pre>

