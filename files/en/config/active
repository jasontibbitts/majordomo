The active configuration setting must be turned on to allow a list to
function normally.  If this setting is turned off, all posted messages,
subscription requests, and other list server commands will be rejected.  
