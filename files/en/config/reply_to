The reply_to setting contains an e-mail address which will be placed in
the "Reply-To" header of posted messages.  If the message already has a
"Reply-To" header, it will be replaced only if the override_reply_to
configuration setting is turned on.
