

The reply category includes the following settings:
   ack_attach_original, allowed_flags, default_flags, default_language, 
   file_search, file_share, inform, nonmember_flags, 
   request_answer, return_subject, welcome, welcome_files

